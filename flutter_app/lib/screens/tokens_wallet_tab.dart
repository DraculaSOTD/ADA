import 'package:flutter/material.dart';

class TokensWalletTab extends StatelessWidget {
  const TokensWalletTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tokens Wallet Tab'),
    );
  }
}
