import 'package:flutter/material.dart';

class ActiveModelsTab extends StatelessWidget {
  const ActiveModelsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Active Models Tab'),
    );
  }
}
