import 'package:flutter/material.dart';

class MyModelsTab extends StatelessWidget {
  const MyModelsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Models Tab'),
    );
  }
}
